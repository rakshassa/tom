<% if address.is_primary %>
	Primary Address:<br>
<% end %>

<%= address.street1 %><br>
<%= if (!address.street2.blank?) then address.street2 + "<br>" end %>
<%= address.city %>,&nbsp<%= address.state %>&nbsp<%= address.zip %><br>
<%= if !address.types.nil? then address.types.reject(&:blank?).to_sentence end %><br>
<br>
