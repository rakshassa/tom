<% if address.is_primary %>
	Primary Address:<br>
<% end %>

<%= address.street1 %><br>

<%= content_tag :div, address.street2 if address.street2? %>
<%= address.city %>,&nbsp<%= address.state %>&nbsp<%= address.zip %><br>

<%= content_tag :div, address.types.reject(&:blank?).to_sentence if !address.types.nil? %>

<br>
